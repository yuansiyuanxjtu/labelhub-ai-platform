# 架构设计

## 分层架构

```mermaid
flowchart TB
  User["浏览器用户"]
  Pages["Next.js App Router 页面"]
  Components["UI Components / shadcn/ui"]
  Api["app/api Route Handlers"]
  Validators["Zod Validators"]
  Auth["Mock RBAC"]
  Services["Service Layer"]
  Workflow["Workflow Status Machine"]
  Agent["QualityGuard Agent"]
  Export["Export Service"]
  Prisma["Prisma Client"]
  DB["SQLite Local DB"]

  User --> Pages
  Pages --> Components
  Pages --> Api
  Api --> Auth
  Api --> Validators
  Api --> Services
  Services --> Workflow
  Services --> Agent
  Services --> Export
  Services --> Prisma
  Prisma --> DB
```

## 主链路时序

```mermaid
sequenceDiagram
  participant Owner as Task Owner
  participant Annotator
  participant Reviewer
  participant API
  participant Service
  participant Agent as QualityGuard
  participant DB as Prisma/SQLite

  Owner->>API: Create task / update formSchema
  API->>Service: validate + permission + call service
  Service->>DB: write Task / FormSchemaVersion / AuditLog
  Annotator->>API: submit annotation
  API->>Service: submitAnnotation()
  Service->>DB: Annotation + Sample SUBMITTED
  Service->>DB: AuditLog
  Reviewer->>API: run AI review
  API->>Service: runAiReview()
  Service->>Agent: reviewWithMeta()
  Agent-->>Service: structured JSON result
  Service->>DB: AiReview + Sample AI_REVIEWED
  Reviewer->>API: submit human review
  API->>Service: submitHumanReview()
  Service->>DB: HumanReview + Sample APPROVED/RETURNED/ESCALATED
  Owner->>API: export dataset
  API->>Service: exportDataset()
  Service->>DB: ExportJob + Sample EXPORTED
```

## 工作流状态图

```mermaid
stateDiagram-v2
  [*] --> PENDING
  PENDING --> ASSIGNED
  ASSIGNED --> IN_PROGRESS
  IN_PROGRESS --> SUBMITTED
  SUBMITTED --> AI_REVIEWING
  AI_REVIEWING --> AI_REVIEWED
  AI_REVIEWED --> HUMAN_REVIEWING
  HUMAN_REVIEWING --> APPROVED
  HUMAN_REVIEWING --> RETURNED
  HUMAN_REVIEWING --> ESCALATED
  RETURNED --> IN_PROGRESS
  APPROVED --> EXPORTED
  EXPORTED --> [*]
```

## 目录职责

- `app/`：页面和 API route。
- `components/`：页面组件、通用 UI、动态表单组件、样本展示和审核组件。
- `lib/services/`：标注、审核、导出、任务和审计日志等业务服务。
- `lib/agents/`：QualityGuard Agent、provider 抽象和 prompt 模板。
- `lib/workflow/`：样本状态机和合法流转。
- `lib/validators/`：核心 API 的 Zod 请求校验。
- `lib/auth/`：本地 mock 当前用户和轻量 RBAC。
- `types/`：formSchema、sample、API 等共享类型。
- `prisma/`：数据库 schema、migration 和 seed。

